from __future__ import annotations

from pathlib import Path
from typing import Any


def run_benchmark_replication(input_dir: Path, output_dir: Path, **kwargs: Any) -> dict[str, Any]:
    raise NotImplementedError(
        "Benchmark Replication workflow is being rebuilt as part of the study reframe."
    )
