from __future__ import annotations

from pathlib import Path
from typing import Any


def run_study_audit(output_dir: Path, **kwargs: Any) -> dict[str, Any]:
    raise NotImplementedError(
        "Study Audit workflow is being rebuilt as part of the study reframe."
    )
