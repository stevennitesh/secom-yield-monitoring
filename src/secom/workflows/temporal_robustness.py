from __future__ import annotations

from pathlib import Path
from typing import Any


def run_temporal_robustness(input_dir: Path, output_dir: Path, **kwargs: Any) -> dict[str, Any]:
    raise NotImplementedError(
        "Temporal Robustness workflow is being rebuilt as part of the study reframe."
    )
