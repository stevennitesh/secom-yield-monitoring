# Historical Reports

These files are tracked historical artifacts from the earlier pre-reframe study lane.
They are useful for context only and are not the active output contract.

Active generated study outputs belong under `runs/<study>/reports/`, with the full
study defaulting to `runs/full_study/reports/`.
