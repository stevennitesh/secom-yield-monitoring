# Runtime dependencies
matplotlib==3.10.9
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.9.0
scipy==1.17.1
# Python >=3.11 compatibility note: skrebate==0.62 publishes a pure py3 wheel
# with Python 3 classifiers and no Requires-Python cap; ReliefF import/fit is
# smoke-tested under the pinned Python 3.12 stack used by CI.
skrebate==0.62

# Development and test dependencies
pytest==9.0.3
pytest-cov==7.1.0
ruff==0.15.15

# Build tooling dependencies
setuptools==82.0.1
wheel==0.47.0
