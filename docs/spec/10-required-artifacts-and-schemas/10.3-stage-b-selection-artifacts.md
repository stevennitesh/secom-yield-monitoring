---
spec_id: "10.3"
title: "Stage B Selection Artifacts"
status: "canonical"
legacy_source:
  - "docs/final_end_to_end_report_strategy_merged.md §10"
applies_to:
  - "Lane B"
  - "Stage A"
  - "Stage B"
used_by_runbooks:
  - "../../report_strategy/runbooks/03_lane_b_stage_a_stage_b.md"
  - "../../report_strategy/runbooks/05_artifacts_and_claim_checks.md"
keywords:
  - stage b
  - timeaware results
  - feature stability
  - model selection
---
# 10.3 Stage B Selection Artifacts

## `reports/timeaware_selector_screening.csv`

1. Columns: `selector`, `n_splits`, `mean_BER`, `std_BER`, `mean_True+`, `mean_True-`, `min_test_fails`.

## `reports/splitwise_timeaware_results.csv`

1. One row per `(selector, outer_fold, seed)`.
2. Columns: `selector`, `outer_fold`, `seed`, `train_window`, `test_window`, `k`, `C`, `scaler`, `n_neighbors` (nullable for non-ReliefF selectors).
3. Columns: `threshold_policy`, `outer_threshold`, `test_fails`, `BER`, `True+`, `True-`.
4. Allowed `threshold_policy` values are fixed: `outer_train_youden_ber_optimal`.

## `reports/stage_b_inner_cv_results.csv`

1. One row per `(selector, outer_fold, seed, k, C, scaler, n_neighbors)` where `n_neighbors` is nullable for non-ReliefF selectors.
2. Columns: `selector`, `outer_fold`, `seed`, `k`, `C`, `scaler`, `n_neighbors` (nullable for non-ReliefF selectors).
3. Columns: `mean_inner_ROC_AUC`, `mean_inner_BER`, `is_selected_config`.
   `mean_inner_ROC_AUC` and `mean_inner_BER` are the unweighted arithmetic mean over the 5 inner validation folds for that `(selector, outer_fold, seed, config)`.
4. Uniqueness constraint: exactly one `is_selected_config=True` row per `(selector, outer_fold, seed)`.

## `reports/timeaware_model_selection.csv`

1. Method-level summary over `(outer_fold x seed)`.
2. Columns: `selector`, `n_folds`, `n_seeds`, `mean_BER`, `std_BER`, `std_per_fold_BER_means`.
3. Columns: `mean_True+`, `std_True+`, `mean_True-`, `std_True-`, `is_primary`, `is_challenger`.
4. Config columns (`k`, `C`, `scaler`, `n_neighbors`) are intentionally excluded because inner-selected configs vary by `(outer_fold, seed)`.
5. No-challenger fallback behavior: if challenger is unavailable, no row has `is_challenger=true`.
6. `std_per_fold_BER_means` definition is fixed: for each `outer_fold=f`, compute `mu_f = mean_seed(BER_{f,seed})`; then take `std_f(mu_f)` with `ddof=1`.
7. `std_BER`, `std_True+`, and `std_True-` are sample standard deviations with `ddof=1` computed over the tuple-level values across all `(outer_fold, seed)` tuples for that selector.

## `reports/seed_stability_summary.csv`

1. One row per `(selector, seed)`.
2. Columns: `selector`, `seed`, `mean_outer_BER`, `std_outer_BER`, `mean_outer_True+`, `mean_outer_True-`.
3. Columns: `modal_k`, `modal_C`, `modal_scaler`, `modal_n_neighbors` (nullable for non-ReliefF selectors), `n_outer_folds`.
4. Mode tie rule: smaller tied `k`, smaller tied `C`, `StandardScaler` over `RobustScaler`; if `selector=='ReliefF'`, then smaller tied `n_neighbors` else `modal_n_neighbors=NA`.
5. `mean_outer_*` and `std_outer_BER` are computed across outer folds for that fixed `seed` (unweighted over folds; `std_outer_BER` uses `ddof=1`).

## `reports/feature_stability_by_seed.csv`

1. Row scope is full transformed-feature universe for each `(selector, seed, outer_fold)`.
2. One row per `(selector, seed, outer_fold, feature_index)`.
3. Columns: `selector`, `seed`, `outer_fold`, `feature_index`, `feature_type`, `selected` where `feature_type in {'value','missing_indicator'}`.
4. `feature_index` uses the same 0-based transformed-feature indexing scheme from [08.2 Feature Stability and Identity](../08-metrics-and-feature-identity/08.2-feature-stability-and-identity.md); it is not a foreign-key constraint because `reports/feature_stability_by_seed.csv` includes features beyond the frozen primary model.
5. `selected=1` iff the feature is among the `k` features selected by the outer-train refit of the Stage B inner-selected config for that `(selector, seed, outer_fold)`; else `0`.
